package app.searchBar;

import app.audio.LibraryEntry;

public class LibraryArtist extends LibraryEntry {
    /**
     * Instantiates a new Library entry.
     *
     * @param name the name
     */
    public LibraryArtist(final String name) {
        super(name);
    }
}
