package app.searchBar;

import app.audio.LibraryEntry;

public class LibraryHost extends LibraryEntry {
    /**
     * Instantiates a new Library entry.
     *
     * @param name the name
     */
    public LibraryHost(final String name) {
        super(name);
    }
}
